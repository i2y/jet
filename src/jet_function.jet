module jet_function
  def to_task()
    match erlang::fun_info(self, :arity)
      case {:arity, 2}
        Task::call(self)
      case {:arity, 1}
        Task::map(self)
    end
  end

  def arity()
    erlang::fun_info(self, :arity)
  end
end
