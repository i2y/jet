module agent_tool_impl
  agent T
    model "ollama:qwen3.6:35b-a3b"
    expose q(x) -> {a: String}
    tool wordcount(text: String) do |t|
      erlang::length(string::tokens(t, " "))
    end
    tool web_search                       # bare (no impl) still works
  end
end
