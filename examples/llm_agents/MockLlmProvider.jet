# Mock implementation of LlmProvider — for testing (does not hit the API)
module MockLlmProvider
  def self.chat(system_prompt, user_message)
    "[Mock] system=" ++ system_prompt ++ " user=" ++ user_message
  end
end
